{
  "name": "sls-az-func-rest-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install",
    "start-local": "npm run build && echo $npm_package_version && export CP_VERSION=$npm_package_version && sls offline ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless-azure-functions": "^2.0.9",
    "serverless-python-requirements": "^5.1.0"
  }
}